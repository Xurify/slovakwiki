/**
 * Lemmas to never publish from frequency lists (quality / pedagogy).
 * Checked by lemma (Slovak) case-insensitively.
 */
export const FREQUENCY_PUBLISH_BLOCKLIST = new Set(["sex", "pohlavie"]);
