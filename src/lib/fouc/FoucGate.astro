---
import { foucHideUntilReadyStyle, type FoucSurface } from "$lib/fouc/gate";

interface Props {
  surface: FoucSurface;
}

const { surface } = Astro.props;
---

<style is:inline set:html={foucHideUntilReadyStyle(surface)}></style>
