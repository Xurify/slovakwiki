/** Class bundles shared by the grammar topic and case pages (home-page editorial style). */

/** The part of a form the pattern is about: an ending, a prefix. */
export const grammarEndingClass = "text-blue-800";

/** Dotted leader between a term and its gloss, as in the home word list. */
export const grammarLeaderClass =
  "min-w-3 flex-1 translate-y-[-0.2em] border-b border-dotted border-slate-300";

/** Small-caps gloss under a hairline, as in "Take a phrase apart". */
export const grammarTokenGlossClass =
  "border-t border-slate-300 pt-2 font-sans text-[0.68rem] font-bold tracking-[0.12em] text-slate-500 uppercase";

/** Large Slovak form in a pattern token. */
export const grammarTokenClass =
  "font-serif text-[clamp(1.55rem,4vw,2.5rem)] leading-none font-semibold tracking-[-0.04em] text-slate-900";

export const grammarProseClass =
  "m-0 max-w-[62ch] font-serif text-[1.12rem] leading-[1.7] text-pretty text-slate-900";

export const grammarNoteClass =
  "m-0 max-w-[66ch] text-[0.95rem] leading-[1.7] text-pretty text-slate-600";
